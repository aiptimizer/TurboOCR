// HostDeviceQueue / HostDeviceEvent — everything is inline in the header (the
// host queue is pure no-op state), so this TU exists only to give the classes a
// translation unit and keep the file layout parallel with the NVIDIA backend.

#include "cpu/queue/host_device_queue.h"

namespace turbo_ocr::cpu {

// (no out-of-line definitions needed)

} // namespace turbo_ocr::cpu
